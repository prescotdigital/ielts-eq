# Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control

# Database
DATABASE_URL="postgresql://username:password@host:5432/database?sslmode=require"

# OpenAI API
OPENAI_API_KEY="sk-..."

# AWS S3 Configuration
AWS_REGION="us-east-1"
AWS_ACCESS_KEY_ID="AKIA..."
AWS_SECRET_ACCESS_KEY="..."
AWS_BUCKET_NAME="your-bucket-name"

# Google OAuth (NextAuth)
GOOGLE_CLIENT_ID="..."
GOOGLE_CLIENT_SECRET="..."

# NextAuth Configuration
NEXTAUTH_SECRET="generate-with: openssl rand -base64 32"
NEXTAUTH_URL="http://localhost:3000"

# Email (Resend)
RESEND_API_KEY="re_..."

# Production Notes:
# - For Vercel deployment, set these in: Settings → Environment Variables
# - NEXTAUTH_URL should be your production domain (e.g., https://your-app.vercel.app)
# - Ensure all secrets are kept secure and never shared publicly
